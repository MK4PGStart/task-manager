# Автотесты на Java

Директория для автотестов на Java.

## Структура

- `src/` - исходный код тестов
- `pom.xml` или `build.gradle` - файл конфигурации проекта

## Примеры фреймворков

- JUnit 5
- TestNG
- Selenium WebDriver (для UI тестов)
- RestAssured (для API тестов)

